import { InfiniteDirective } from './infinite.directive';

describe('InfiniteDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteDirective();
    expect(directive).toBeTruthy();
  });
});
